<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-sdk tools:overrideLibrary="org.torproject.jni, info.guardianproject.tor" />

    <!-- Permisos para escaneo e instalación de APKs -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" 
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Declaración de hardware para Android TV -->
    <uses-feature 
        android:name="android.hardware.touchscreen" 
        android:required="false" />
    <uses-feature 
        android:name="android.software.leanback" 
        android:required="false" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:banner="@drawable/vortex_banner"
        android:supportsRtl="true"
        android:theme="@style/Theme.VortexTV"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:banner="@drawable/vortex_banner"
            android:theme="@style/Theme.VortexTV">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".activities.FileScannerActivity"
            android:exported="false"
            tools:ignore="LockedOrientationActivity"
            android:theme="@style/Theme.VortexTV" />
            
        <activity
            android:name=".activities.FirewallAppDetailActivity"
            android:exported="true"
            android:theme="@style/Theme.VortexTV" />
        
        <activity
            android:name=".activities.SystemAppsActivity"
            android:exported="false"
            tools:ignore="LockedOrientationActivity"
            android:theme="@style/Theme.VortexTV" />
        
        <activity
            android:name=".activities.FirewallActivity"
            android:exported="true"
            tools:ignore="LockedOrientationActivity"
            android:theme="@style/Theme.VortexTV" />

        <activity
            android:name=".activities.SettingsActivity"
            android:exported="true"
            tools:ignore="LockedOrientationActivity"
            android:theme="@style/Theme.VortexTV" />

        <!-- Tor embedded service -->
        <service
            android:name=".tor.TorService"
            android:exported="true"
            android:foregroundServiceType="dataSync" />

        <!-- FileProvider for In-App Updates -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/updater_file_paths" />
        </provider>
    </application>

</manifest>