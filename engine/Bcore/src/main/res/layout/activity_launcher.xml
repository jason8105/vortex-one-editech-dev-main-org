<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_launcher_splash">

    <!-- Contenedor central vertical -->
    <LinearLayout
        android:id="@+id/ll_center_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="32dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Contenedor del Icono con Tarjeta y Badge Tor -->
        <FrameLayout
            android:id="@+id/card_icon_container"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:background="@drawable/bg_splash_icon_card"
            android:padding="16dp">

            <ImageView
                android:id="@+id/iv_icon"
                android:layout_width=" match_parent"
                android:layout_height="match_parent"
                android:scaleType="fitCenter"
                android:contentDescription="@string/app_icon"
                android:src="@drawable/ic_launcher" />

            <!-- Badge Tor (visible si la app se abre con Tor) -->
            <LinearLayout
                android:id="@+id/layout_tor_splash_badge"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:layout_gravity="top|end"
                android:layout_margin="-4dp"
                android:background="@drawable/bg_tor_badge_splash"
                android:elevation="6dp"
                android:gravity="center"
                android:visibility="gone">

                <ImageView
                    android:layout_width="18dp"
                    android:layout_height="18dp"
                    android:src="@drawable/ic_tor_onion_splash"
                    android:contentDescription="Tor Routing Active" />
            </LinearLayout>
        </FrameLayout>

        <!-- Nombre de la aplicación -->
        <TextView
            android:id="@+id/tv_app_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:fontFamily="sans-serif-medium"
            android:gravity="center"
            android:maxWidth="400dp"
            android:maxLines="2"
            android:text="Aplicación"
            android:textColor="@color/splash_text_primary"
            android:textSize="22sp"
            android:textStyle="bold" />

        <!-- Indicador de carga animado -->
        <ProgressBar
            android:id="@+id/pb_loading"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_marginTop="24dp"
            android:indeterminate="true"
            android:indeterminateTint="@color/splash_accent" />

        <!-- Texto de estado descriptivo -->
        <TextView
            android:id="@+id/tv_loading_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:fontFamily="sans-serif-light"
            android:gravity="center"
            android:text="Iniciando aplicación..."
            android:textColor="@color/splash_text_secondary"
            android:textSize="14sp" />

        <!-- Contenedor del Nodo de Salida Tor (Visible si Tor está activo) -->
        <LinearLayout
            android:id="@+id/layout_tor_exit_info"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:background="@drawable/bg_tor_exit_pill"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingStart="14dp"
            android:paddingTop="8dp"
            android:paddingEnd="14dp"
            android:paddingBottom="8dp"
            android:visibility="gone">

            <TextView
                android:id="@+id/tv_tor_flag"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="16sp"
                android:text="🧅" />

            <TextView
                android:id="@+id/tv_tor_exit_details"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:fontFamily="sans-serif-medium"
                android:text="Estableciendo circuito seguro..."
                android:textColor="#C7D2FE"
                android:textSize="13sp" />
        </LinearLayout>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
